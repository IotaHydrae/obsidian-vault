
关联类型经常被拿来与泛型（Generic Parameters）做比较。简单来说，它是定义在 `trait` 内部的“类型占位符”。

### 1. 为什么要用关联类型？

我们看一个经典的例子：**迭代器 (Iterator)**。 如果使用普通泛型，定义可能长这样：

```rust
trait Iterator<Item> {
    fn next(&mut self) -> Option<Item>;
}
```